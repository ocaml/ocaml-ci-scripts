(jbuild_version 1)

(executable
  ((name        ci_opam)
   (modules     (ci_opam))
   (public_name ci-opam)
;   (flags        (:standard -cclib -static))
   (libraries    (yorick jsonm))))

(executable
  ((name        travis_mirage)
   (modules     (travis_mirage))
   (public_name travis-mirage)
   (libraries    (yorick))))
